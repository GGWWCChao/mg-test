<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.majiang.mgtest.mapper.QuestionMapper">

    <insert id="create" parameterType="com.majiang.mgtest.model.Question">
        insert into question(title, description, mgt_modified, create,tag)
        values (#{title},#{description},#{mgtModified},#{create},#{tag})
    </insert>
</mapper>